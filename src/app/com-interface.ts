export interface PaginationData {
    totalCount:number,
    maxRow:number,
    currentRow:number
}
export interface SFormat {
    data:any,
	list:any
	status:string,
	cause:string,
	totalcount:number
}